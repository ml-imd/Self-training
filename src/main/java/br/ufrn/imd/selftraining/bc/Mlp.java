/*
 * package br.ufrn.imd.selftraining.bc;
 * 
 * import br.ufrn.imd.selftraining.enums.BaseClassifierType; import
 * br.ufrn.imd.selftraining.enums.ClassifierType;
 * 
 * public class Mlp extends Classifier{
 * 
 * private double l; //0.3 private double m; //0.2 private int n; //500 private
 * int v; //0 private int s; //0 private int e; private
 * 
 * -G GUI will be opened. (Use this to bring up a GUI).
 * 
 * -A Autocreation of the network connections will NOT be done. (This will be
 * ignored if -G is NOT set)
 * 
 * -B A NominalToBinary filter will NOT automatically be used. (Set this to not
 * use a NominalToBinary filter).
 * 
 * -H <comma separated numbers for nodes on each layer> The hidden layers to be
 * created for the network. (Value should be a list of comma separated Natural
 * numbers or the letters 'a' = (attribs + classes) / 2, 'i' = attribs, 'o' =
 * classes, 't' = attribs .+ classes) for wildcard values, Default = a).
 * 
 * -C Normalizing a numeric class will NOT be done. (Set this to not normalize
 * the class if it's numeric).
 * 
 * -I Normalizing the attributes will NOT be done. (Set this to not normalize
 * the attributes).
 * 
 * -R Reseting the network will NOT be allowed. (Set this to not allow the
 * network to reset).
 * 
 * -D Learning rate decay will occur. (Set this to cause the learning rate to
 * decay).
 * 
 * 
 * 
 * public Mlp(){ super(); this.name =
 * BaseClassifierType.MULTI_LAYER_PECEPTRON.getInfo(); this.classifierType
 * =ClassifierType.BASE_CLASSIFIER; } }
 */